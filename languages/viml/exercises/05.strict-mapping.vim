nmap dd jddk

" Above mapping will run forever because recursive command
" To prevent this, always use noremap, nnoremap, vnoremap, inoremap

nnoremap dd jddk

" One again: never use map, use noremap instead
